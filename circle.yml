machine:
  node:
    version: v5.11.0
dependencies:
  pre:
    - npm install npm@3.x -g
  override:
    - npm install
test:
  override:
    - npm test
#   post:
    # - bash <(curl -s https://codecov.io/bash)